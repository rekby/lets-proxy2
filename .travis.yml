language: go

go:
  - "stable"
  - "master"

env:
  global:
    - GO111MODULE=on
  matrix:
    - GOFLAGS=-mod=vendor
    - GOFLAGS=

matrix:
  exclude:
  - go: "master"
    env:
      - GOFLAGS=-mod=vendor

services:
  - docker

script:
  - bash tests/prepare.sh
  - go test -v ./...