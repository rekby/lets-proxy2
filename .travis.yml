language: go

go:
  - "stable"
  - "master"

env:
  global:
    - GOMODULE111=on
    - VERSION=$TRAVIS_TAG commit $TRAVIS_COMMIT
  matrix:
    - GOFLAGS=-mod=vendor
    - GOFLAGS=

services:
  - docker

script:
  - bash tests/prepare.sh
  - go test -v ./...